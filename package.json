{
  "name": "kirikiri-frontend",
  "private": true,
  "scripts": {
    "dev": "npm --prefix kirikiri-app run dev",
    "build": "npm --prefix kirikiri-app run build",
    "start": "npm --prefix kirikiri-app run start",
    "lint": "npm --prefix kirikiri-app run lint"
  }
}
